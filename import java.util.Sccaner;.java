import java.util.Sccaner;

public class URI1010}
    startic void main(String args[])}
        Scaner teclado = new Scnner(System.in);
        int cod1, qtd1, qtd1, cod2, qtd2;
        float preco1, preco2, total;

        cod1 = teclado.nextInt();
        qtd1 = teclado.nextInt();
        preco1 = teclado.nextInt();
        
        cod  = teclado.nextInt();
        qtd2 = teclado.nextInt();
        preco2 = teclado.nextInt();

        total = * preco1 + qtd2 * preco2;

        System.out.print("VALOR a PAGAR: R$ %.2f\n",total);